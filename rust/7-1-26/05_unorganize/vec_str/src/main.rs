fn main() {
    let mut Vs: Vec<String> = vec!["Vextor".to_string(), "Horizon".to_string()];
    // ["Vextor", "Horizon"]; = &str

    Vs.push("VextorHorizon".to_string()); Vs.push("Jiraput".to_string()); Vs.push("Jiranan".to_string());


    println!("{:?}", Vs);
}
